apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-dubhe.fullname" . }}
  labels:
    {{- include "nginx-dubhe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-dubhe.selectorLabels" . | nindent 4 }}
