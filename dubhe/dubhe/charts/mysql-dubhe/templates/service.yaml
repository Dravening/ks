apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-dubhe.fullname" . }}
  labels:
    {{- include "mysql-dubhe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysql-dubhe.selectorLabels" . | nindent 4 }}
