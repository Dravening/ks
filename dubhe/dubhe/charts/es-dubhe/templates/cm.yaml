apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "es-dubhe.fullname" . }}
  labels:
    app: {{ template "es-dubhe.fullname" . }}
    chart: '{{ .Chart.Name }}-{{ .Chart.Version }}'
    release: '{{ .Release.Name }}'
    heritage: '{{ .Release.Service }}'
data:
  elasticsearch.yml: |-
