apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "es-dubhe.fullname" . }}-test-connection"
  labels:
    {{- include "es-dubhe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "es-dubhe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
