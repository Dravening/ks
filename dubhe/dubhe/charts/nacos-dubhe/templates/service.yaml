apiVersion: v1
kind: Service
metadata:
  name: {{ include "nacos-dubhe.fullname" . }}
  labels:
    {{- include "nacos-dubhe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nacos-dubhe.selectorLabels" . | nindent 4 }}
