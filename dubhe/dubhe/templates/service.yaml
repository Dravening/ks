apiVersion: v1
kind: Service
metadata:
  name: {{ include "dubhe.fullname" . }}
  labels:
    {{- include "dubhe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dubhe.selectorLabels" . | nindent 4 }}
