apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-studio.fullname" . }}
  labels:
    {{- include "mysql-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysql-studio.selectorLabels" . | nindent 4 }}
