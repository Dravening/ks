apiVersion: v1
kind: Service
metadata:
  name: {{ include "dt-studio-web.fullname" . }}
  labels:
    {{- include "dt-studio-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodeport }}
  selector:
    {{- include "dt-studio-web.selectorLabels" . | nindent 4 }}
