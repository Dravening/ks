apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataservice-tianneng.fullname" . }}
  labels:
    {{- include "dataservice-tianneng.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataservice-tianneng.selectorLabels" . | nindent 4 }}
