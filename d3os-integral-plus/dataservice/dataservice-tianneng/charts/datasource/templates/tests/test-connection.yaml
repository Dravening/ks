apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datasource.fullname" . }}-test-connection"
  labels:
    {{- include "datasource.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datasource.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
