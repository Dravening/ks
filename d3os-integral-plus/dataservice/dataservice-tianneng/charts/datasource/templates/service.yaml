apiVersion: v1
kind: Service
metadata:
  name: {{ include "datasource.fullname" . }}
  labels:
    {{- include "datasource.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datasource.selectorLabels" . | nindent 4 }}
