apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gateway-web.fullname" . }}-test-connection"
  labels:
    {{- include "gateway-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gateway-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
