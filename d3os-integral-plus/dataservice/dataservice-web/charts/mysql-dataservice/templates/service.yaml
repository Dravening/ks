apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-dataservice.fullname" . }}
  labels:
    {{- include "mysql-dataservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysql-dataservice.selectorLabels" . | nindent 4 }}
