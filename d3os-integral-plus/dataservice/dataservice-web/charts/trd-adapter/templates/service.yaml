apiVersion: v1
kind: Service
metadata:
  name: {{ include "trd-adapter.fullname" . }}
  labels:
    {{- include "trd-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trd-adapter.selectorLabels" . | nindent 4 }}
