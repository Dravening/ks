apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataservice-web.fullname" . }}
  labels:
    {{- include "dataservice-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataservice-web.selectorLabels" . | nindent 4 }}
