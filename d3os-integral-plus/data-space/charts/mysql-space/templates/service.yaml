apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-space.fullname" . }}
  labels:
    {{- include "mysql-space.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysql-space.selectorLabels" . | nindent 4 }}
