apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql-space.fullname" . }}-test-connection"
  labels:
    {{- include "mysql-space.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysql-space.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
